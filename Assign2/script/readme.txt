This folder includes all the scripts for running the experiment. 
bfs.sh contains the script for running BFS_opt for all the approaches and two inputs graphs with different cache sizes (256kB, 1MB, 4MB).
mst.sh contains the script for running MST_opt for all the approaches and two inputs graphs with different cache sizes (256kB, 1MB, 4MB).

